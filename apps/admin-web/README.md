# Admin Web Placeholder

The admin web dashboard is intentionally deferred for the MVP.

Backend contracts in `backend/functions` and the data model in `docs/data-model.md` are shaped so a future React admin dashboard can be added without changing the underlying Firestore schema or callable interfaces.
